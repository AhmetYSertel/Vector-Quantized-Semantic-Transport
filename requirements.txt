numpy>=1.24
scikit-learn>=1.3
sentence-transformers>=2.2      # RealEncoder (torch) — scripts/ exp9 pipeline
fastembed>=0.8                  # FastEmbedEncoder (ONNX) — experiments/ intent path
pyyaml>=6.0
# optional, for the live-LLM hooks (exp9):
# openai>=1.0
pytest>=7.0                     # tests only

# For an exact-reproduction environment (the versions that produced the
# committed results/ and artifacts/), install from requirements-lock.txt instead.
